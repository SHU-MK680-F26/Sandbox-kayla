# Secrets and credentials — never commit these
.env
.env.*
*.pem
*.key
*.p12
*token*
*secret*
credentials.json
service-account*.json

# Raw data exports that may contain personal information
data/raw/
*.xlsx
contacts*.csv
subscribers*.csv
*export*.csv

# Local Claude Code state (settings.json IS committed; local overrides are not)
.claude/settings.local.json

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
